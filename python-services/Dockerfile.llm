FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY llm_service.py .

EXPOSE 8008

CMD ["uvicorn", "llm_service:app", "--host", "0.0.0.0", "--port", "8008"]
